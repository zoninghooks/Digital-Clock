# Digital Clock App

Depending on what you return in `App.jsx`, you will get a different clock.

The background can be changed to whatever you like, as long as it is in `src/assets` and named `background.jpg`.

## Clock Options

- `DigitalClock.jsx` - 12-hour time (AM/PM)
- `DigitalClock2.jsx` - 24-hour time